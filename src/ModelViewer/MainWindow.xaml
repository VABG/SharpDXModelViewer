<Window x:Class="ModelViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:ModelViewer.Controls"
        Title="3D Model Viewer" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!-- Outer grid: Menu (top) | Content (middle) | StatusBar (bottom) -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- Menu bar -->
            <RowDefinition Height="*" />     <!-- Main content -->
            <RowDefinition Height="Auto" />  <!-- Status bar -->
        </Grid.RowDefinitions>
        <!-- Menu bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Click="Exit_Click" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Reset Camera" Click="ResetCamera_Click" />
            </MenuItem>
        </Menu>

        <!-- Main content area with resizable left/right panels -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="180" />    <!-- Left panel -->
                <ColumnDefinition Width="5" />                      <!-- Left splitter -->
                <ColumnDefinition Width="*" MinWidth="300" />      <!-- 3D viewport -->
                <ColumnDefinition Width="5" />                      <!-- Right splitter -->
                <ColumnDefinition Width="250" MinWidth="180" />    <!-- Right panel -->
            </Grid.ColumnDefinitions>
        <!-- Scene models panel (left side) -->
            <DockPanel Grid.Column="0" Margin="4">
            <controls:SceneModelPanel x:Name="ScenePanel"
                                      DockPanel.Dock="Top"
                                      Margin="0"
                                      Height="Auto" />
            <controls:ModelTransformPanel x:Name="TransformPanel"
                                          DockPanel.Dock="Top"
                                          Margin="0,4,0,0"
                                          Height="Auto" />
        </DockPanel>

            <!-- Left GridSplitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="#2D2D2D"
                          BorderBrush="#505050"
                          BorderThickness="1,0,1,0" />
            <!-- The 3D rendering surface -->
            <ContentControl x:Name="RenderHost" Grid.Column="2" />

            <!-- Right GridSplitter -->
            <GridSplitter Grid.Column="3"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="#2D2D2D"
                          BorderBrush="#505050"
                          BorderThickness="1,0,1,0" />

            <!-- Light direction control panel (right side) -->
            <controls:LightControlPanel x:Name="LightPanel"
                                        Grid.Column="4"
                                        Margin="5"
                                        Height="Auto" />
        </Grid>

        <!-- Status bar -->
        <controls:StatusBar x:Name="StatusBar" Grid.Row="2" />
    </Grid>
</Window>